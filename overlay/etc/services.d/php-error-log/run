#!/usr/bin/with-contenv bash

set -euo pipefail
export BCG_E="${PWD##*/}/$(basename ${0})"


#======================================================================================================================
# Forward PHP errors to Docker.
#======================================================================================================================

bcg-forward ${PWD##*/} ${PHP_INI_ERROR_LOG}
